// here i will fetch the posts from the server (get endpoint). which is getting the posts from the database

export default function PostsPage() {
  // we need useEffect to fetch the data
  //we need a function to fetchData() {
  // fetch(url)
  // parse data in json
  // console log data so you see what it looks like
  // }

  // ! once you are finished, replace local host urls with render server url
  return (
    <>
      <h1>posts</h1>
      {/* i need to render my posts here
      A suggestion is use conditional rendering here */}
      <h2>yey</h2>
    </>
  );
}
